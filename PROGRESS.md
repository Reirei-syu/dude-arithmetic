# 项目进度

## 当前阶段

- Execution

## 当前任务

- 增强出题配置：位数范围、答案范围、出题数量模式

## 本次修改

### 1. 数字 A / C 的位数
- 快捷卡片从 1-5 缩减到 1-3 位数
- 新增手动录入范围输入框（最小位数、最大位数），支持负数
- 负数位数 = 生成负的 N 位数（如 -2 表示 -99 ~ -10）
- 快捷选择和手动范围可叠加使用
- 界面实时显示当前生效的位数组合

### 2. 答案 D 改为范围
- 从单一上限改为 [下限, 上限] 双输入，支持负数
- 下限必须小于上限

### 3. 出题数量双模式
- 模式切换按钮：「题目数量」/「练习天数」
- 题目数量模式：默认 64 题，直接输入数量
- 练习天数模式：输入天数，每天 64 题，实时显示总题数

### 4. 输入框空值处理
- 所有 number input 不再强制显示 0
- 删除内容后输入框保持空白，使用 parseIntSafe 安全解析

### 5. 核心逻辑
- generateNum 支持负数位数
- generateProblems 全面重写，正确处理 A/C 含负数、D 为范围的情况
- 除法跳过除数为 0、正确处理负数除法
- 可行性检查兼容负数值范围

## 影响范围

- src/App.tsx（主文件，几乎全面改动）
- 主页面配置区 A/B/C/D/E 全部有变化
- 生成逻辑、打印逻辑适配新数据结构

## 任务进度百分比

- 100%

## 方案路径

- 无

## 验证结果

- `npm run lint`：通过
- `npm run build`：通过
